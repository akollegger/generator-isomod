language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-isomod' ]; then cd .. && eval "mv $currentfolder generator-isomod" && cd generator-isomod; fi

