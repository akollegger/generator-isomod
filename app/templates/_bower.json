{
  "name": "<%= _.slugify(moduleName) %>",
  "version": "<%= moduleVersion %>",
  "dependencies": {
  },
  "devDependencies": {
    "almond": "0.2.7"
  },
  "main": [
    "dist/<%= _.slugify(moduleName) %>.js"
  ]
}